#!/usr/bin/perl

use JSON::Schema;

open schemah, "<service_manager.json";
my @schema = <schemah>;
close schemah;

my %hash;
$hash{fig} = 'tvoyu mat*';
$hash{blin} = 12;

$validator = JSON::Schema->new(@schema);
my $result = $validator->validate(\%hash);

